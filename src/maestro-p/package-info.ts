// Version constant for the maestro-p wrapper binary.
// Tracks the binary's own contract — bumped independently of Maestro's app version
// because phases 2+ will pin a known-good maestro-p version inside the app.

export const VERSION = '0.1.0';
