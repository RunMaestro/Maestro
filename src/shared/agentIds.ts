/**
 * Legacy import path for built-in agent IDs.
 *
 * The values are derived solely from the shared registry; this module owns no
 * list or validation behavior.
 */
export { AGENT_IDS, isValidAgentId } from './agentRegistry';
export type { AgentId } from './agentRegistry';

